# MongoDB Collections

- `audit_logs`: user activity, privileged actions, signal overrides
- `camera_event_logs`: AI inference traces and frame processing metadata
- `notification_logs`: downstream delivery attempts for SMS, email, and push
- `analytics_rollups`: denormalized dashboard aggregates
- `incident_timelines`: per-incident event history and annotations

