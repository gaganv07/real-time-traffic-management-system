apiVersion: v1
kind: Service
metadata:
  name: traffic-platform
  namespace: traffic-system
spec:
  selector:
    app: traffic-platform
  ports:
    - name: http
      port: 8000
      targetPort: 8000
